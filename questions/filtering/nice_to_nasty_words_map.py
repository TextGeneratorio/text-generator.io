# replace some output words with more explicit ones, this needs configuration per project depending upon a use case
# this replacing only happens if we detect bad words initially.
nice_to_nasty_words_map = {
}

